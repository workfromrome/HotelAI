# Leave unset for local dev (Vite proxies /api to localhost:8000, see vite.config.js).
# For a Netlify deploy, set this in Site settings > Environment variables to your
# deployed backend's URL, e.g. https://your-backend.onrender.com/api
VITE_API_BASE_URL=
